继承和初始化

加载顺序：

        a、System.out.println("Beetle constructor");
        b、Beetle b=new Beetle();
    1、当前类是否继承某父类
    2、执行父类的类属性和类方法，再执行当前类的类属性和类方法
    3、b进行实例化当前类：执行父类的构造方法，再执行当前类的构造方法

    初始化顺序总结：当前类的基类，当前类的静态属性，当前类的静态方法，如果进行了当前类的实例化，
                  则进入当前类的成员变量、成员方法(按照声明顺序初始化)、构造函数。